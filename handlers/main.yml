---
- name: Restart Gitea compose deployment
  ansible.builtin.command:
    cmd: docker compose --ansi never --progress plain restart
    chdir: "{{ gitea_docker_path }}"
  changed_when: true
  listen: restart gitea
