{{ ansible_managed | ansible.builtin.comment }}

{% for item in gitea_docker_env | ansible.builtin.dict2items | sort(attribute='key') %}
{{ item.key }}={{ item.value }}
{% endfor %}

# USER_UID and USER_GID are managed by ansible.
USER_GID={{ gitea_docker_group_info.gid }}
USER_UID={{ gitea_docker_owner_info.uid }}
